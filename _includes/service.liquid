<div class="bg-white rounded-lg shadow-md p-6 text-center dark:bg-gray-800">
    <div class="flex justify-center items-center mb-4">
        {% include {{ include.svg }} %}
    </div>
    <h3 class="text-lg font-medium text-gray-900 dark:text-white">{{ include.name }}</h3>
    <p class="mt-2 text-gray-600">{{ include.description }}</p>
</div>
